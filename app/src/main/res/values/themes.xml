<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ============================================
         Andclaw Material Design 3 深色主题
         ============================================ -->

    <style name="Theme.Andclaw" parent="Theme.Material3.Dark.NoActionBar">
        <!-- 主色 -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- 次要色 -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- 第三色 -->
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorOnTertiary">@color/on_tertiary</item>

        <!-- 背景 -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>

        <!-- 错误 -->
        <item name="colorError">@color/error</item>
        <item name="colorErrorContainer">@color/error_container</item>
        <item name="colorOnError">@color/on_error</item>

        <!-- 状态栏 -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- 文字 -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Material 3 组件样式 -->
        <item name="materialCardViewStyle">@style/Widget.Andclaw.Card</item>
        <item name="materialButtonStyle">@style/Widget.Andclaw.Button</item>
        <item name="textInputStyle">@style/Widget.Andclaw.TextInput</item>
    </style>

    <!-- 透明主题 (用于悬浮窗等) -->
    <style name="Theme.Andclaw.Transparent" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- ============================================
         组件样式
         ============================================ -->

    <!-- 卡片样式 -->
    <style name="Widget.Andclaw.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="strokeColor">@color/card_border</item>
        <item name="strokeWidth">1dp</item>
        <item name="rippleColor">@color/primary</item>
    </style>

    <!-- 按钮样式 -->
    <style name="Widget.Andclaw.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/surface_variant</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="cornerRadius">@dimen/radius_md</item>
    </style>

    <style name="Widget.Andclaw.Button.Primary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="cornerRadius">@dimen/radius_md</item>
    </style>

    <style name="Widget.Andclaw.Button.Secondary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/secondary</item>
        <item name="android:textColor">@color/on_secondary</item>
        <item name="cornerRadius">@dimen/radius_md</item>
    </style>

    <!-- 文本输入样式 -->
    <style name="Widget.Andclaw.TextInput" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/primary</item>
        <item name="hintTextColor">@color/text_secondary</item>
    </style>

    <!-- ============================================
         聊天气泡样式
         ============================================ -->

    <!-- 用户气泡 -->
    <style name="BubbleUser">
        <item name="cardBackgroundColor">@color/bubble_user</item>
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">2dp</item>
        <item name="rippleColor">@color/primary</item>
    </style>

    <!-- AI 气泡 -->
    <style name="BubbleAI">
        <item name="cardBackgroundColor">@color/bubble_ai</item>
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">1dp</item>
        <item name="strokeColor">@color/bubble_ai_border</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- 系统气泡 -->
    <style name="BubbleSystem">
        <item name="cardBackgroundColor">@color/bubble_system</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>
    </style>

    <!-- ============================================
         工具栏样式
         ============================================ -->

    <style name="Widget.Andclaw.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/background</item>
        <item name="titleTextColor">@color/text_primary</item>
        <item name="subtitleTextColor">@color/text_secondary</item>
        <item name="navigationIconTint">@color/text_primary</item>
    </style>

    <!-- ============================================
         标题样式
         ============================================ -->

    <style name="TextAppearance.Andclaw.Title" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:letterSpacing">0.01</item>
    </style>

    <style name="TextAppearance.Andclaw.Body" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Andclaw.Caption" parent="TextAppearance.Material3.LabelSmall">
        <item name="android:textColor">@color/text_secondary</item>
    </style>
</resources>